resources:
  repositories:
  - repository: build
    type: github
    name: Haipa/build
    endpoint: haipa


extends:
  template: pipelines/templates/dotnetclient.yml@build
  parameters:
    withCmdlet: false